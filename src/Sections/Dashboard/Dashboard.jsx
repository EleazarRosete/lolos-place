import styles from './Dashboard.module.css';

function Dashboard() { 

    return (
        <section className={styles.section}>
            <div>
                <h2>Dashboard</h2>
            </div>
        </section>
    );
}

export default Dashboard;
